<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Permissions matching the Meta Spatial SDK 0.12 starter sample.
         INTERNET is required by NetworkedAssetLoader (used by the SDK
         even when we don't fetch assets ourselves). MODIFY_AUDIO_SETTINGS
         is needed by AAudio low-latency mode on some Quest builds. -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.MODIFY_AUDIO_SETTINGS" />

    <!-- Quest 3 hardware requirements -->
    <uses-feature
        android:name="android.hardware.vr.headtracking"
        android:required="true"
        android:version="1" />
    <uses-feature
        android:glEsVersion="0x00030002"
        android:required="true" />

    <!-- Passthrough is part of MVP scope (rhythm gameplay involves arm
         movement; the user must see the real room for safety). -->
    <uses-feature
        android:name="com.oculus.feature.PASSTHROUGH"
        android:required="true" />

    <!-- Hand tracking optional for MVP; controllers cover all input. -->
    <uses-feature
        android:name="oculus.software.handtracking"
        android:required="false" />

    <application
        android:allowBackup="false"
        android:icon="@android:drawable/sym_def_app_icon"
        android:label="@string/app_name"
        android:supportsRtl="false">

        <!-- Tells the Meta Horizon OS launcher this is a Quest 3 title. -->
        <meta-data
            android:name="com.oculus.supportedDevices"
            android:value="quest3" />

        <activity
            android:name=".MainActivity"
            android:configChanges="screenSize|screenLayout|orientation|keyboardHidden|keyboard|navigation|uiMode"
            android:exported="true"
            android:launchMode="singleTask"
            android:theme="@android:style/Theme.Black.NoTitleBar.Fullscreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
                <category android:name="com.oculus.intent.category.VR" />
            </intent-filter>
        </activity>
    </application>
</manifest>
